export type Lang = 'ru' | 'en' | 'uz';
export type User = { id:number; username:string|null; first_name:string|null; language:Lang; mode:string; mode_arg:string|null; current_chat_id:string|null; last_model_key:string|null; search_model_key:string|null; previous_chat_model_key:string|null; previous_chat_web_search_state:number; default_role_key:string|null; is_blocked:number; block_reason:string|null; bot_blocked:number; processing_until:string|null; ui_message_id:number|null; created_at:string; last_seen_at:string };
export type Chat = { id:string; user_id:number; seq:number; title:string; model_key:string; role_key:string|null; custom_role:string|null; is_archived:number; created_at:string; updated_at:string };
export type Model = { model_key:string; name:string; family:string; provider:string; model_id:string; type:'chat'|'search'|'image'|'stt'|'tts'|'document'; tier:'daily'|'advanced'; cost:number; is_active:number; is_free:number; supports_text:number; supports_images:number; supports_audio:number; supports_documents:number; max_input:number|null; max_output:number|null; config:string|null; sort:number; created_at:string };
export type Role = { role_key:string; name_ru:string; name_en:string; prompt:string; sort:number; is_active:number };
export type Plan = { plan_key:string; kind:'subscription'|'points'; title_ru:string; title_en:string; points:number; duration_days:number|null; price_minor:number; currency:string; is_active:number; sort:number };
export type PlanPaymentPrice = { plan_key:string; provider:string; currency:string; amount:number; is_active:number; updated_at:string };
export type Order = { id:string; user_id:number; plan_key:string; kind:string; title_ru:string; title_en:string; amount_minor:number; currency:string; status:'pending'|'paid'|'cancelled'|'expired'|'review'|'refunded'; provider:string|null; provider_order_id:string|null; payment_url:string|null; created_at:string; paid_at:string|null; expires_at:string|null };
export type PointHold = { id:string; user_id:number; free_amount:number; paid_amount:number; free_reset_at:string|null; status:'held'|'captured'|'released'; ref:string; created_at:string; finished_at:string|null };
export type ChatAiSettings = { user_id:number; chat_id:string; model_key:string; reasoning_mode:'fast'|'deep'|'max'; web_search_enabled:number; updated_at:string };
